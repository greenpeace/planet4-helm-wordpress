apiVersion: v1
kind: Service
metadata:
  name: {{ template "openresty.fullname" . }}
  labels:
    app: {{ template "openresty.name" . }}
    chart: {{ template "openresty.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "openresty.name" . }}
    release: {{ .Release.Name }}
