{{- if and .Values.exim.password .Values.exim.password -}}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-exim"
  labels:
    app: "{{ .Release.Name }}-exim"
    chart: {{ template "wordpress.chart" . }}
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  username: {{ .Values.exim.username | b64enc | quote }}
  password: {{ .Values.exim.password | b64enc | quote }}
{{- end -}}
